package main

/**
  线程间进行通信 ，go 使用channel 方式
 */
func main() {
		//channel 创建方式
	  //c1:=make(chan int )
	  //c2:=make(chan int,10) //创建一个缓冲长度为10的channel
	  //c3:=make(chan string)
	  //c4:=make(chan interface{})
	  //写入值
     //ch:=make(chan int)
     //ch<-10
	//取出值
     //num:=<-ch
	//上面的只是理论，不能在实际中运行
	//如果不按照该规则使用，
	//则会引起经典的Golang错误fatal error: all goroutines are asleep - deadlock!：





}
